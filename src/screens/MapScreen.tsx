import { useEffect, useState } from 'react';
import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { campusService } from '../services/campusService';
import { colors } from '../theme/colors';
import { Building } from '../types/domain';
export function MapScreen() { const [buildings, setBuildings] = useState<Building[]>([]); useEffect(() => { campusService.getBuildings().then(setBuildings); }, []); return <ScrollView style={styles.container} contentContainerStyle={styles.content}><Text style={styles.title}>지도 Placeholder</Text><View style={styles.mapBox}><Text style={styles.mapText}>지도 API 연동 전 mock 좌표 기반 영역</Text>{buildings.map((b, index) => <View key={b.id} style={[styles.pin, { top: 70 + index * 58, left: 50 + index * 72 }]}><Text style={styles.pinText}>{b.shortName}</Text></View>)}</View><Text style={styles.note}>추후 Naver/Kakao/Google Maps adapter를 service 레이어에 연결할 수 있게 화면은 placeholder로 유지합니다.</Text></ScrollView>; }
const styles = StyleSheet.create({ container: { flex: 1, backgroundColor: colors.background }, content: { padding: 20, paddingTop: 56 }, title: { fontSize: 28, fontWeight: '800', color: colors.text }, mapBox: { height: 420, borderRadius: 28, backgroundColor: '#DCEBFF', marginTop: 20, overflow: 'hidden', borderWidth: 1, borderColor: colors.border }, mapText: { color: colors.muted, textAlign: 'center', marginTop: 28, fontWeight: '700' }, pin: { position: 'absolute', backgroundColor: colors.primary, borderRadius: 999, paddingHorizontal: 12, paddingVertical: 8 }, pinText: { color: 'white', fontWeight: '800' }, note: { color: colors.muted, marginTop: 18, lineHeight: 21 } });
