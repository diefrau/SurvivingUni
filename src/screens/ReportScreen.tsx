import { useEffect, useState } from 'react';
import { Alert, Pressable, ScrollView, StyleSheet, Text, TextInput } from 'react-native';
import { campusService } from '../services/campusService';
import { colors } from '../theme/colors';
import { Building } from '../types/domain';
export function ReportScreen() { const [buildings, setBuildings] = useState<Building[]>([]); const [placeName, setPlaceName] = useState(''); const [description, setDescription] = useState(''); useEffect(() => { campusService.getBuildings().then(setBuildings); }, []); const submit = async () => { const buildingId = buildings[0]?.id ?? 'unknown'; const result = await campusService.submitReport({ placeName, buildingId, category: 'study', description }); Alert.alert('제보 접수', `mock id: ${result.id}`); setPlaceName(''); setDescription(''); }; return <ScrollView style={styles.container} contentContainerStyle={styles.content}><Text style={styles.title}>생존 장소 제보</Text><Text style={styles.help}>서버 연결 전까지 입력값은 mock service로 전달됩니다.</Text><TextInput value={placeName} onChangeText={setPlaceName} placeholder="장소 이름" style={styles.input} /><TextInput value={description} onChangeText={setDescription} placeholder="어떤 점이 유용한가요?" multiline style={[styles.input, styles.textarea]} /><Pressable style={styles.button} onPress={submit}><Text style={styles.buttonText}>제보 보내기</Text></Pressable></ScrollView>; }
const styles = StyleSheet.create({ container: { flex: 1, backgroundColor: colors.background }, content: { padding: 20, paddingTop: 56 }, title: { fontSize: 28, fontWeight: '900', color: colors.text }, help: { color: colors.muted, marginTop: 10, lineHeight: 21 }, input: { backgroundColor: colors.card, borderColor: colors.border, borderWidth: 1, borderRadius: 16, padding: 14, marginTop: 16, fontSize: 16 }, textarea: { minHeight: 140, textAlignVertical: 'top' }, button: { backgroundColor: colors.primary, borderRadius: 16, padding: 16, alignItems: 'center', marginTop: 18 }, buttonText: { color: 'white', fontWeight: '900', fontSize: 16 } });
